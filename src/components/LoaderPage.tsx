import Loader from '../core-components/Loader/Loader'

function LoaderPage() {
    return (
        <div>
            <h2>Loader</h2>
            <Loader size="extra_large" />
        </div>
    )
}

export default LoaderPage
